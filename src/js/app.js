// const add = () =>{};

// document.querySelector(text);
// addEventListener("add", add);

// let text =querySelector(text).value

// let save = [];

// save.push( save (add));


//Create
const InputText = document.getElementById('text');
const btnAddText =document.getElementById('btnAdd');

btnAddText.addEventListener('click');

const textUser = InputText.value;

//me falta enviar los datos a la tabla

//Read

const returnText = (textUser) => {
const tableUser =document.getElementById('userText');
return tableUser;
}
returnText();

//update



//delete

const borrar = (userText) => {
    let index = textUser.findIndex((item, index, array) => {})
    userText.splice (index,1)
}